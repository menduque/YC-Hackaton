# Copy to `.env` (gitignored). Server-side only — never expose to the browser bundle.

# --- Stedi ------------------------------------------------------------------
# Generate a TEST key at https://portal.stedi.com/app/settings/developer/api-keys
# (Mode: Test). Test keys only work with the documented mock requests — no PHI/PII,
# nothing reaches a real payer, no charges.
STEDI_API_KEY=
STEDI_ACCOUNT_ID=

# --- Moss (moss.dev) --------------------------------------------------------
# Sign up at https://moss.dev for a free project_id / project_key.
MOSS_PROJECT_ID=
MOSS_PROJECT_KEY=
